import React,{useEffect,useState} from 'react'; import {FlatList,Text} from 'react-native'; import {riskApi} from '../api/client'; import {Card,H,P,Screen} from '../components/UI';
export default function MyReportsScreen(){const[d,setD]=useState<any[]>([]);useEffect(()=>{riskApi.myReports().then(setD).catch(()=>{})},[]);return <Screen><H>My reports</H><P>Track moderation status. Pending reports should not be represented as established facts.</P><FlatList data={d} keyExtractor={(x:any)=>String(x.id)} ListEmptyComponent={<P>No reports yet.</P>} renderItem={({item}:any)=><Card><Text style={{fontWeight:'800'}}>{item.name}</Text><P>{item.status} · {item.submittedAt?new Date(item.submittedAt).toLocaleDateString():''}</P></Card>}/></Screen>}
