import React,{useEffect,useState} from 'react'; import {ScrollView,Text} from 'react-native'; import {riskApi} from '../api/client'; import {Card,H,P,Screen} from '../components/UI';
export default function TermsScreen(){const[d,setD]=useState<any>();useEffect(()=>{riskApi.terms().then(setD).catch(()=>{})},[]);return <ScrollView><Screen><H>Terms & safeguards</H><Card><P>{d?.disclaimer||'Reports are allegations until reviewed. Evidence may be challenged. Subjects must have a meaningful correction/dispute path.'}</P>{(d?.clauses||[]).map((x:string,i:number)=><Text key={i}>• {x}</Text>)}</Card><Card><Text style={{fontWeight:'800'}}>Demo safety rules</Text><P>No harassment, threats, doxxing, revenge reporting, discriminatory targeting, knowingly false claims, or publication of unnecessary personal data. Do not upload intimate images, passwords, financial credentials or unrelated third-party data.</P></Card></Screen></ScrollView>}
