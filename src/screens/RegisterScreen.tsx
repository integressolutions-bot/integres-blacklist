import React,{useState} from 'react'; import {Alert,ScrollView} from 'react-native'; import {Button,Card,Field,H,P,Screen} from '../components/UI'; import {useAuth} from '../context/AuthContext';
export default function RegisterScreen(){const{register}=useAuth();const[n,setN]=useState('');const[e,setE]=useState('');const[p,setP]=useState('');return <ScrollView><Screen><H>Create account</H><P>Accounts created here are standard users. Elevated access should only be granted administratively.</P><Card><Field placeholder="Full name" value={n} onChangeText={setN}/><Field placeholder="Email" autoCapitalize="none" value={e} onChangeText={setE}/><Field placeholder="Password" secureTextEntry value={p} onChangeText={setP}/><Button title="Register" onPress={async()=>{try{await register({fullName:n,email:e.trim(),password:p})}catch(x:any){Alert.alert('Registration failed',x.message)}}}/></Card></Screen></ScrollView>}
