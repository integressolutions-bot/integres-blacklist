import React,{useEffect,useState} from 'react'; import {ScrollView,Text} from 'react-native'; import {riskApi} from '../api/client'; import {Card,H,P,Screen} from '../components/UI';
export default function TermsScreen(){const[d,setD]=useState<any>();useEffect(()=>{riskApi.terms().then(setD).catch(()=>{})},[]);return <ScrollView><Screen><H>Blacklist rules & safeguards</H><Card><Text style={{fontWeight:'900'}}>The product is Blacklist. Review is the safeguard.</Text><P>Integres Blacklist is an accountability and due-diligence platform. Reports can be submitted, reviewed, challenged, corrected and resolved. A user report is an allegation unless independently established.</P></Card><Card><Text style={{fontWeight:'900'}}>Core rules</Text><P>• New reports remain non-public while required review is pending.
• Public search should expose only publication-eligible information and avoid unnecessary personal data.
• Subjects must have a meaningful response/dispute route.
• A dispute can reopen review and may suspend public eligibility.
• Administrative fees cover processing only; they do not prove wrongdoing, guarantee publication or buy deletion.
• Correction/removal requests are reviewed rather than automatically granted.</P></Card><Card><P>{d?.disclaimer||'Use the service in good faith and provide evidence that can be assessed.'}</P>{(d?.clauses||[]).map((x:string,i:number)=><Text key={i}>• {x}</Text>)}</Card><Card><Text style={{fontWeight:'900'}}>Prohibited use</Text><P>No harassment, threats, revenge reporting, discriminatory targeting, doxxing, knowingly false claims, intimate images, passwords, financial credentials or unrelated third-party personal data.</P></Card></Screen></ScrollView>}
