import React,{useState} from 'react'; import {Alert,ScrollView,Switch,Text,View} from 'react-native'; import {riskApi} from '../api/client'; import {Button,Card,Field,H,P,Screen} from '../components/UI';
export default function DisputeScreen({route}:any){const[n,setN]=useState('');const[e,setE]=useState('');const[s,setS]=useState('');const[med,setMed]=useState(true);const[prac,setPrac]=useState(true);const go=async()=>{try{const f=new FormData();f.append('claimantName',n);f.append('claimantEmail',e);f.append('disputeType','ACCURACY_OR_CONTEXT');f.append('summary',s);f.append('country','NG');f.append('termsVersion','blacklist-terms-v1');f.append('mediationRequested',String(med));f.append('practitionerInvolvementRequested',String(prac));f.append('practitionerType','MEDIATOR');const d=await riskApi.dispute(route.params.id,f);Alert.alert('Dispute opened',d.message||'Your response has been recorded.')}catch(x:any){Alert.alert('Could not submit',x.message)}};return <ScrollView><Screen><H>Dispute or correction</H><P>Use this route if you are the subject, representative, or otherwise have a legitimate basis to challenge accuracy, completeness, context or identity.</P><Card><Field placeholder="Your full name" value={n} onChangeText={setN}/><Field placeholder="Email" autoCapitalize="none" value={e} onChangeText={setE}/><Field multiline style={{minHeight:130,textAlignVertical:'top'}} placeholder="Explain what is inaccurate or missing and the correction you seek" value={s} onChangeText={setS}/><View style={{flexDirection:'row',gap:10,alignItems:'center'}}><Switch value={med} onValueChange={setMed}/><Text>Request mediation</Text></View><View style={{flexDirection:'row',gap:10,alignItems:'center'}}><Switch value={prac} onValueChange={setPrac}/><Text>Request practitioner involvement</Text></View><Button title="Submit dispute" onPress={go}/></Card></Screen></ScrollView>}
